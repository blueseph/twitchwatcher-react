import React from 'react';

function StreamFilterBar() {
  return (
    <div className="filter-bar">
      <input className="filter" type="text" />
    </div>);
}

StreamFilterBar.propTypes = {};

export { StreamFilterBar };
